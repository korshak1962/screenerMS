server:
  port: 8083
  address: 0.0.0.0
spring:
  datasource:
    url: jdbc:mysql://mysql-container:3306/screenerMS
    username: root
    password: admin1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always